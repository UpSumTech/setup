{
  "service": {
    "name": "postgres-server",
    "tags": ["9.1"],
    "address": "EXTERNAL_IP",
    "port": EXTERNAL_PORT,
    "id": "SERVICE_ID",
    "checks": [
      {
        "id": "postgres-check",
        "name": "Postgres check",
        "script": "/usr/local/bin/postgres-check.sh",
        "interval": "30s"
      }
    ]
  }
}
